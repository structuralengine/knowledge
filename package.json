{
  "name": "structuralcode",
  "main": "index.js",
  "scripts": {
    "copy": "cpx -C ./src/**/*.{svg,png,jpg} ./src/.vuepress/public",
    "src:dev": "SET NODE_OPTIONS=--openssl-legacy-provider && vuepress dev src",
    "src:build": "SET NODE_OPTIONS=--openssl-legacy-provider && vuepress build src"
  },
  "devDependencies": {
    "@vuepress/plugin-active-header-links": "^1.9.9",
    "@vuepress/plugin-blog": "^1.9.4",
    "cpx": "^1.5.0",
    "vuepress": "^1.9.9"
  }
}
